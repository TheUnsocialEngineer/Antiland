from setuptools import setup, find_packages

setup(
    name="Antiland",
    version="0.73",
    packages=find_packages(),
    install_requires=[
        "requests",
        "base64",
        "requests",
        "json",
        "requests",
        "base64",
        "requests",
        "threading",
        "num2words",
        "datetime",
        "inflect"
        ],
)
